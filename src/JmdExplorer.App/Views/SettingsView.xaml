<UserControl x:Class="JmdExplorer.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" MaxWidth="640" HorizontalAlignment="Left">
            <TextBlock Text="Settings" Style="{StaticResource HeaderText}"/>

            <Border Background="{StaticResource BgPanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Scan defaults" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4">
                        <TextBlock Text="Default minimum string length:" Width="240" VerticalAlignment="Center"/>
                        <TextBox Width="60" Text="{Binding DefaultMinStringLength}"/>
                    </StackPanel>
                    <CheckBox Content="Scan UTF-16 strings by default" IsChecked="{Binding DefaultScanUtf16}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource BgPanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Logging" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding LogDirectory}" Style="{StaticResource MonoText}" TextWrapping="Wrap"/>
                    <Button Content="Open Log Folder" Command="{Binding OpenLogFolderCommand}" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource BgPanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="6" Padding="16">
                <StackPanel>
                    <TextBlock Text="About" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding AppVersion, StringFormat=Version {0}}" Style="{StaticResource SubtleText}"/>
                    <TextBlock Text="{Binding About}" TextWrapping="Wrap" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
