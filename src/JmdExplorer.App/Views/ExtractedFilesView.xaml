<UserControl x:Class="JmdExplorer.App.Views.ExtractedFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Raw Block Extractor" Style="{StaticResource HeaderText}"/>

        <Border Grid.Row="1" Background="{StaticResource BgPanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,10">
            <WrapPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,16,6">
                    <TextBlock Text="Start:" VerticalAlignment="Center" Margin="0,0,6,0" Style="{StaticResource SubtleText}"/>
                    <TextBox Width="120" Text="{Binding RangeStart, UpdateSourceTrigger=PropertyChanged}" ToolTip="e.g. 0x100"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,16,6">
                    <TextBlock Text="End:" VerticalAlignment="Center" Margin="0,0,6,0" Style="{StaticResource SubtleText}"/>
                    <TextBox Width="120" Text="{Binding RangeEnd, UpdateSourceTrigger=PropertyChanged}" ToolTip="blank = EOF"/>
                </StackPanel>
                <Button Content="Extract Range" Style="{StaticResource AccentButton}" Command="{Binding ExtractRangeCommand}"
                        IsEnabled="{Binding HasFile}"/>
                <Button Content="Extract All Detected Regions" Command="{Binding ExtractAllRegionsCommand}"
                        IsEnabled="{Binding HasFile}"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Background="#332A1F1F" CornerRadius="6" Padding="12" Margin="0,0,0,10"
                BorderBrush="{StaticResource WarnBrush}" BorderThickness="1">
            <TextBlock TextWrapping="Wrap" Foreground="{StaticResource WarnBrush}" FontSize="12"
                       Text="Extracted blocks are RAW binary data + a JSON manifest. They are not guaranteed to be directly usable as a model, texture, or configuration file. No decoding is performed."/>
        </Border>

        <Border Grid.Row="3" Background="{StaticResource BgPanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="6">
            <DockPanel>
                <Border DockPanel.Dock="Top" Background="{StaticResource BgRaisedBrush}" Padding="12,8"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                    <TextBlock Text="Extracted files (this session)" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"/>
                </Border>
                <ListBox ItemsSource="{Binding ExtractedFiles}" Margin="6">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="4,3">
                                <TextBlock Text="&#xE8B7;" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource AccentBrush}"
                                           Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding}" Style="{StaticResource MonoText}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
