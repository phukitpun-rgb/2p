<UserControl x:Class="JmdExplorer.App.Views.DecoderPluginsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Decoder Plugins" Style="{StaticResource HeaderText}"/>
        <TextBlock Grid.Row="1" Style="{StaticResource SubtleText}" Margin="0,0,0,12" TextWrapping="Wrap"
                   Text="Plugins are discovered via dependency injection. A plugin marked 'Not Available' is a placeholder extension point — it will never fabricate decoded output."/>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Plugins}" MaxHeight="260">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Plugin" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="Auto"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto"/>
                <DataGridTextColumn Header="Supported Format" Binding="{Binding SupportedFormat}" Width="2*"/>
                <DataGridTemplateColumn Header="">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Run" Padding="10,2" Margin="2"
                                    Command="{Binding DataContext.RunCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="3" Background="{StaticResource BgPanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="6" Margin="0,12,0,0" Padding="14">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Last plugin result" Foreground="{StaticResource AccentBrush}"
                           FontWeight="SemiBold" Margin="0,0,0,8"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBox Text="{Binding LastResult, Mode=OneWay}" IsReadOnly="True" BorderThickness="0"
                             Background="Transparent" Foreground="{StaticResource TextBrush}"
                             FontFamily="{StaticResource MonoFont}" TextWrapping="Wrap" AcceptsReturn="True"/>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
